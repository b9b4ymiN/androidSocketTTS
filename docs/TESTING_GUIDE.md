# คู่มือการทดสอบแอพ Reeman TTS

## วิธีที่ 1: ทดสอบบนอุปกรณ์ Android จริง (แนะนำที่สุด)

### ข้อกำหนดเบื้องต้น
- มือถือ/แท็บเล็ต Android (API 26+ / Android 8.0+)
- สายเคเบิล USB
- เปิด Developer Options และ USB Debugging

### ขั้นตอนการเตรียมอุปกรณ์

1. **เปิด Developer Mode:**
   - ไปที่ Settings > About Phone
   - แตะ "Build Number" 7 ครั้ง
   - จะขึ้น "You are now a developer!"

2. **เปิด USB Debugging:**
   - ไปที่ Settings > Developer Options
   - เปิด "USB Debugging"

3. **ติดตั้ง Google TTS (ถ้ายังไม่มี):**
   - เปิด Play Store
   - ค้นหา "Google Text-to-Speech"
   - กด Install
   - ดาวน์โหลดข้อมูลภาษาไทย (Settings > Languages > Add Language > Thai)

4. **ตั้งค่า TTS Engine:**
   - Settings > Accessibility > Text-to-Speech
   - เลือก "Google Text-to-Speech" เป็น Preferred engine
   - ทดสอบกด "Listen to an example" (จะพูดภาษาอังกฤษ)

### วิธีติดตั้งและรันแอพ

```bash
# 1. ต่อมือถือเข้ากับคอมพิวเตอร์ด้วยสาย USB

# 2. ตรวจสอบว่าเครื่องเชื่อมต่อแล้ว
adb devices
# ควรเห็นอุปกรณ์ของคุณ เช่น:
# List of devices attached
# ABC123DEF456    device

# 3. Build และติดตั้งแอพ
cd /home/user/androidSocketTTS
./gradlew installDebug

# 4. เปิดแอพบนมือถือ (หรือเปิดด้วยมือจากหน้าจอ)
adb shell am start -n com.factory.reemantts/.MainActivity

# 5. ดูข้อมูล log แบบ real-time
adb logcat -c  # ล้าง log เก่า
adb logcat | grep "RemanTTS"
```

### การทดสอบ TTS

1. **เปิดแอพ Reeman TTS บนมือถือ**

2. **กด "Initialize TTS"**
   - รอประมาณ 1-2 วินาที
   - สังเกต Status เปลี่ยนจาก "Not Initialized" → "Ready"
   - ดู Activity Log ควรเห็น:
     ```
     Creating TTS Manager...
     ✓ TTS initialized successfully
     ✓ Thai language configured
     ✓ Speech rate: 0.9
     ✓ Pitch: 1.0
     ```

3. **ทดสอบเสียงอัตโนมัติ - กด "Test Thai Speech (Auto)"**
   - จะได้ยินเสียงพูดภาษาไทย 4 ประโยค:
     - "สวัสดีครับ"
     - "ทดสอบระบบเสียงภาษาไทย"
     - "หนึ่ง สอง สาม สี่ ห้า"
     - "ระบบพร้อมใช้งาน"
   - สังเกต Status เปลี่ยนเป็น "Speaking..." จนจบ
   - ดู log เห็นข้อความแต่ละประโยค

4. **ทดสอบข้อความกำหนดเอง**
   - พิมพ์ข้อความภาษาไทยในช่อง "Custom Text Input"
   - ตัวอย่าง: "สวัสดีตอนเช้า วันนี้อากาศดีมาก"
   - กด "Speak"
   - จะได้ยินเสียงพูดตามที่พิมพ์

5. **ทดสอบปุ่ม Stop**
   - กด "Test Thai Speech (Auto)" อีกครั้ง
   - ระหว่างที่กำลังพูด กด "Stop"
   - เสียงควรหยุดทันที

### การแก้ไขปัญหาเบื้องต้น

**ปัญหา: ไม่มีเสียง**
```bash
# ตรวจสอบว่า TTS engine ทำงานหรือไม่
adb shell settings get secure tts_default_synth

# ทดสอบ TTS จาก command line
adb shell "echo 'สวัสดีครับ' | am start -n com.google.android.tts/com.google.android.apps.speech.tts.googletts.SettingsActivity"
```

**ปัญหา: Initialization Failed**
- ตรวจสอบ log:
```bash
adb logcat | grep -i "TTS"
```
- ลองติดตั้ง Google TTS ใหม่
- ตรวจสอบว่าดาวน์โหลดข้อมูลภาษาไทยแล้ว

---

## วิธีที่ 2: ใช้ Android Emulator (AVD)

### ข้อกำหนดเบื้องต้น
- ติดตั้ง Android Studio หรือ Android SDK Command Line Tools
- RAM อย่างน้อย 8GB (แนะนำ 16GB)
- CPU รองรับ Virtualization (Intel VT-x หรือ AMD-V)

### ขั้นตอนการสร้าง Emulator

1. **ติดตั้ง Android SDK (ถ้ายังไม่มี)**
```bash
# ดาวน์โหลด Command Line Tools จาก:
# https://developer.android.com/studio#command-tools

# หรือติดตั้ง Android Studio จาก:
# https://developer.android.com/studio
```

2. **สร้าง AVD ด้วย command line**
```bash
# ดู System Images ที่มี
sdkmanager --list | grep system-images

# ดาวน์โหลด System Image (API 34)
sdkmanager "system-images;android-34;google_apis_playstore;x86_64"

# สร้าง AVD
avdmanager create avd -n ReemanTTS_Test -k "system-images;android-34;google_apis_playstore;x86_64" -d pixel_5

# รัน Emulator
emulator -avd ReemanTTS_Test
```

3. **ติดตั้ง Google TTS บน Emulator**
```bash
# หลังจาก emulator เปิดขึ้นมาแล้ว
# เปิด Play Store บน emulator
# ค้นหาและติดตั้ง "Google Text-to-Speech"
# ดาวน์โหลดข้อมูลภาษาไทย
```

4. **ติดตั้งและรันแอพ**
```bash
# Build และติดตั้ง
./gradlew installDebug

# รัน
adb shell am start -n com.factory.reemantts/.MainActivity
```

### ข้อจำกัดของ Emulator
- ⚠️ **เสียงอาจไม่ชัดเท่าอุปกรณ์จริง**
- ⚠️ **ใช้ทรัพยากรเครื่องค่อนข้างสูง**
- ⚠️ **ต้องติดตั้ง Google Play Services และ TTS Engine ก่อน**

---

## วิธีที่ 3: ทดสอบบน VS Code (ใช้ Terminal)

VS Code สามารถใช้ได้ แต่ต้องใช้ผ่าน Terminal เท่านั้น (ไม่มี GUI เหมือน Android Studio)

### ขั้นตอน

1. **เปิด VS Code และเปิด Terminal (Ctrl + `)**

2. **Navigate ไปยังโฟลเดอร์โปรเจกต์**
```bash
cd /home/user/androidSocketTTS
```

3. **ตรวจสอบว่ามีอุปกรณ์เชื่อมต่ออยู่**
```bash
adb devices
```

4. **Build แอพ**
```bash
./gradlew assembleDebug
```

5. **ติดตั้งบนอุปกรณ์**
```bash
./gradlew installDebug
```

6. **เปิดแอพ**
```bash
adb shell am start -n com.factory.reemantts/.MainActivity
```

7. **ดู log แบบ real-time**
```bash
adb logcat -c
adb logcat | grep "RemanTTS"
```

### VS Code Extensions ที่แนะนำ

ติดตั้ง extension เหล่านี้ใน VS Code:
- **Kotlin Language** - สำหรับ syntax highlighting
- **Android Debug Bridge (ADB)** - สำหรับควบคุม ADB
- **Gradle for Java** - สำหรับรัน Gradle tasks

---

## วิธีที่ 4: ใช้ Android Studio (เหมาะสำหรับ Development)

ถ้าต้องการประสบการณ์ที่ดีที่สุด แนะนำให้ใช้ Android Studio:

1. เปิด Android Studio
2. File > Open > เลือก `/home/user/androidSocketTTS`
3. รอให้ Gradle sync เสร็จ
4. เลือกอุปกรณ์ (device/emulator) จาก dropdown
5. กด Run (▶️) หรือ Shift+F10
6. ดู Logcat ได้ทันทีใน IDE

---

## การทดสอบบน Reeman Robot จริง

เมื่อพร้อมจะทดสอบบนหุ่นยนต์ Reeman:

### ขั้นตอน

1. **เชื่อมต่อ Reeman Robot กับคอมพิวเตอร์**
```bash
# ใช้ ADB ผ่าน WiFi (ถ้า Reeman รองรับ)
adb connect <REEMAN_IP_ADDRESS>:5555

# หรือเชื่อมต่อผ่าน USB
adb devices
```

2. **ติดตั้งแอพ**
```bash
./gradlew installDebug
```

3. **ตรวจสอบเสียงของ Reeman**
```bash
# ปรับระดับเสียง
adb shell media volume --stream 3 --set 15

# ทดสอบเสียง
adb shell media volume --show
```

4. **รันแอพและทดสอบ**
```bash
adb shell am start -n com.factory.reemantts/.MainActivity
```

---

## สคริปต์อำนวยความสะดวก

สร้างไฟล์สำหรับรันทดสอบง่ายๆ:

### test-app.sh
```bash
#!/bin/bash

echo "🤖 Reeman TTS Testing Script"
echo "=============================="

# ตรวจสอบอุปกรณ์
echo "📱 Checking connected devices..."
adb devices -l

# Build แอพ
echo "🔨 Building app..."
./gradlew assembleDebug

# ติดตั้ง
echo "📲 Installing app..."
./gradlew installDebug

# เปิดแอพ
echo "🚀 Launching app..."
adb shell am start -n com.factory.reemantts/.MainActivity

# แสดง log
echo "📋 Showing logs (Ctrl+C to exit)..."
adb logcat -c
adb logcat | grep "RemanTTS"
```

### วิธีใช้
```bash
chmod +x test-app.sh
./test-app.sh
```

---

## เช็คลิสต์การทดสอบ Phase 1

- [ ] TTS Engine เริ่มต้นสำเร็จ
- [ ] ภาษาไทยทำงานถูกต้อง
- [ ] เสียงชัดเจน ไม่สะดุด
- [ ] Speech Rate (0.9) เหมาะสม
- [ ] Auto Test พูด 4 ประโยคติดต่อกัน
- [ ] Custom Text Input ทำงาน
- [ ] ปุ่ม Stop หยุดเสียงได้
- [ ] Log แสดงผลครบถ้วน
- [ ] ไม่มี Error ใน Logcat

---

## Tips สำหรับการทดสอบเสียง

1. **ปรับ Speech Rate** ถ้าเสียงเร็วเกินไป:
   - แก้ไข `Config.kt`:
   ```kotlin
   const val SPEECH_RATE = 0.7f  // ลดลงจาก 0.9
   ```

2. **ตรวจสอบระดับเสียง**:
   ```bash
   # เช็คระดับเสียง
   adb shell media volume --show

   # ปรับเสียงสูงสุด
   adb shell media volume --stream 3 --set 15
   ```

3. **บันทึกเสียงเพื่อทดสอบ**:
   ```bash
   # บันทึกเสียงจากอุปกรณ์ (ถ้ารองรับ)
   adb shell screenrecord --audio-source mic /sdcard/test.mp4
   # กด Ctrl+C เพื่อหยุด
   adb pull /sdcard/test.mp4
   ```

---

## สรุป

**วิธีที่แนะนำสำหรับ Phase 1:**
1. ใช้มือถือ Android จริง (ง่าย ได้ยินเสียงชัดเจน) ⭐
2. รันผ่าน VS Code Terminal ก็ได้ แต่ต้องใช้คำสั่ง adb
3. ถ้าไม่มีเครื่องจริง ใช้ Emulator (ช้ากว่า แต่ใช้งานได้)

**วิธีที่แนะนำสำหรับ Production (Phase 7):**
- ทดสอบบน Reeman Robot จริงเท่านั้น
- ทดสอบในสภาพแวดล้อมโรงงานจริง
- ทดสอบเสียงกลบเสียงรบกวนจากโรงงาน
