# วิธีทดสอบแอพ Reeman TTS (ภาษาไทย)

## วิธีที่เร็วที่สุด - ใช้สคริปต์อัตโนมัติ ⚡

```bash
# รันคำสั่งเดียวจบ
./quick-test.sh
```

จะทำให้:
- Build แอพอัตโนมัติ
- ติดตั้งลงเครื่อง Android
- เปิดแอพให้เลย

---

## วิธีทดสอบแบบละเอียด (มี log)

```bash
# รันสคริปต์ทดสอบแบบเต็ม
./test-app.sh
```

จะได้:
- ข้อมูลการ build
- ข้อมูลการติดตั้ง
- Log แบบ real-time

---

## วิธีทดสอบแบบ Manual (VS Code)

### 1. เปิด Terminal ใน VS Code (Ctrl + `)

### 2. ตรวจสอบว่ามีอุปกรณ์เชื่อมต่อ
```bash
adb devices
```

ถ้าเห็น:
```
List of devices attached
ABC123DEF456    device
```
แสดงว่าพร้อมแล้ว! ✅

ถ้าไม่มี:
- เสียบสาย USB เข้ากับมือถือ
- เปิด USB Debugging (Settings > Developer Options)
- กด "Allow" บนมือถือเมื่อมี popup ถาม

### 3. Build และติดตั้ง
```bash
./gradlew installDebug
```

### 4. เปิดแอพบนมือถือ
```bash
adb shell am start -n com.factory.reemantts/.MainActivity
```

### 5. ทดสอบบนมือถือ
1. กด "Initialize TTS"
2. รอ 1-2 วินาที จนขึ้น "Ready"
3. กด "Test Thai Speech (Auto)"
4. จะได้ยินเสียงพูดภาษาไทย 4 ประโยค

### 6. ดู Log (ถ้าต้องการ)
```bash
adb logcat | grep "RemanTTS"
```

---

## ถ้าไม่มีมือถือ Android - ใช้ Emulator

### วิธีสร้าง Emulator ง่ายๆ

1. **ติดตั้ง Android Studio** (ถ้ายังไม่มี)
   - ดาวน์โหลด: https://developer.android.com/studio

2. **เปิด Android Studio > AVD Manager**
   - Tools > Device Manager
   - Create Device
   - เลือก Pixel 5
   - Download System Image (API 34)
   - Finish

3. **เปิด Emulator**
   - กด ▶️ ที่ AVD Manager
   - รอจน emulator เปิดขึ้นมา

4. **ติดตั้ง Google TTS**
   - เปิด Play Store บน emulator
   - ค้นหา "Google Text-to-Speech"
   - ติดตั้งและดาวน์โหลดภาษาไทย

5. **รันแอพ**
   ```bash
   ./quick-test.sh
   ```

---

## เช็คลิสต์การทดสอบ

เมื่อรันแอพแล้ว ให้ทดสอบตามนี้:

### ✅ ทดสอบพื้นฐาน
- [ ] กด "Initialize TTS" → ขึ้น "Ready"
- [ ] กด "Test Thai Speech" → ได้ยินเสียงไทย
- [ ] เสียงชัดเจน ไม่สะดุด
- [ ] พูดครบ 4 ประโยค

### ✅ ทดสอบ Custom Text
- [ ] พิมพ์: "สวัสดีตอนเช้า"
- [ ] กด "Speak"
- [ ] ได้ยินเสียงตามที่พิมพ์

### ✅ ทดสอบปุ่ม Stop
- [ ] กด "Test Thai Speech"
- [ ] ระหว่างพูด กด "Stop"
- [ ] เสียงหยุดทันที

### ✅ ทดสอบ Log
- [ ] Activity Log แสดงข้อความ
- [ ] เห็น "✓ TTS initialized successfully"
- [ ] เห็น "Speaking: สวัสดีครับ"

---

## แก้ปัญหาเบื้องต้น

### ❌ ปัญหา: adb: command not found

**วิธีแก้:**
```bash
# ติดตั้ง Android SDK Platform Tools
# หรือใช้ Android Studio (มี adb มาให้เลย)
```

### ❌ ปัญหา: No device connected

**วิธีแก้:**
1. เสียบสาย USB ให้แน่น
2. เปิด USB Debugging บนมือถือ
3. ลอง `adb devices` อีกครั้ง

### ❌ ปัญหา: ไม่มีเสียง

**วิธีแก้:**
1. เช็คว่าเปิดเสียงบนมือถือ
2. ติดตั้ง Google TTS:
   - Play Store > "Google Text-to-Speech"
   - ดาวน์โหลดข้อมูลภาษาไทย
3. ตั้งค่า TTS Engine:
   - Settings > Accessibility > Text-to-Speech
   - เลือก Google TTS

### ❌ ปัญหา: Initialization Failed

**วิธีแก้:**
```bash
# ดู error ใน log
adb logcat | grep -i "TTS\|error"

# ลองติดตั้ง Google TTS ใหม่
# ตรวจสอบว่าดาวน์โหลดภาษาไทยแล้ว
```

---

## คำสั่งที่ใช้บ่อย

```bash
# ดูอุปกรณ์ที่เชื่อมต่อ
adb devices

# Build แอพ
./gradlew assembleDebug

# ติดตั้งแอพ
./gradlew installDebug

# เปิดแอพ
adb shell am start -n com.factory.reemantts/.MainActivity

# ดู log
adb logcat | grep "RemanTTS"

# ลบแอพออก
adb uninstall com.factory.reemantts

# Restart ADB (ถ้ามีปัญหา)
adb kill-server
adb start-server
```

---

## สรุป

**แนะนำสำหรับผู้เริ่มต้น:**
1. ใช้มือถือ Android จริง (ง่ายที่สุด)
2. รัน `./quick-test.sh` (คำสั่งเดียวจบ)
3. กด "Initialize TTS" และ "Test Thai Speech"
4. ฟังเสียงภาษาไทย

**ถ้าต้องการทดสอบแบบละเอียด:**
- รัน `./test-app.sh` (จะมี log ให้ดู)

**ถ้าไม่มีมือถือ:**
- ใช้ Android Studio + Emulator

---

## ติดต่อสอบถาม

หากมีปัญหาในการทดสอบ:
1. ดู Log: `adb logcat | grep "RemanTTS"`
2. อ่านคู่มือฉบับเต็ม: `docs/TESTING_GUIDE.md`
3. ตรวจสอบว่าติดตั้ง Google TTS และภาษาไทยแล้ว

---

**Happy Testing! 🚀**
